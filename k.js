const animals = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h',];

const findIndex = function() {
    const animalSearch = JSON.stringifytringify.animal;

    if (animalSearch === animals.values) {
        console.log(animals.indexOf)
    }
    console.log(animalSearch);
}